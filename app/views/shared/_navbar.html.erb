<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to image_tag("alt-fuel.png", alt: "logo", class: "brand-pic"), root_path, class: "navbar-brand" %>
      <%= link_to "AltFuelFindr", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <div class="nav navbar-nav navbar-left navbar-brand navbar-welcome">
        <% if current_user %>
          Welcome, <%= "#{current_user.first_name} #{current_user.last_name}!" %>
        </div>
        <div class="nav navbar-collapse navbar-right">
            <%= button_to "Logout", logout_path %>
          <% else %>
            <%= link_to "Sign in with Google", "/auth/google" %>
          <% end %>
        </div>
    </div>
  </div>
</nav>
